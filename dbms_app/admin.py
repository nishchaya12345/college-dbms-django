# MongoEngine documents don't plug into Django's admin site the way
# Django ORM models do, so there's nothing to register here.
# The dbms_app/views.py JSON API and the `run_demo` management command
# are the intended ways to interact with the data for this project.
